Allora sinceramente potrei creare una classe POINT dove posso definire un vettore di lunghezza N
dove tale lunghezza viene inserita dall'utente nel file che poi viene letto da GetPot.
In tale classe potrei avere una funzione per calcolare la norma euclidea tra due POINT e oltretutto
potrebbe essere utile per quanto riguarda la computazione del valore della nostra funzione e del suo
gradiente in tale POINT.

In particolare successivamente alla classe POINT, vorrei creare una classe FUNCTION in cui possa 
inserire la mia funzione che viene inizialmente letta da GetPot da file per essere convertita in una
stringa ed infine con muParserX trasformata in un oggetto che poi possa essere valutato tramite 
un'altra funzione che utilizza la classe POINT.

Infine vorrei realizzare una classe GRADIENT DESCENT in grado di effettuare il GD su una funzione
fornita dall'utente ed in grado di implementare diverse funzioni relative al learning rate
segnate tutte sul PDF del professore. 

Se implemento tutto questo dovrebbe essere un buon lavoro sinceramente.

N.B
g++ -c GradientDescent.cpp -o GradientDescent.o
g++ GradientDescent.o Point.cpp -o GradientDescent

			up here
